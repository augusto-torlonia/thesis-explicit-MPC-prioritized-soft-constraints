# thesis-explicit-MPC-prioritized-soft-constraints
I developed an Explicit MPC algorithm in Simulink that prevents controller failure under infeasible states. The controller automatically guarantees a continuous control action by systematically relaxing soft constraints based on a hierarchical priority logic, maintaining system stability even under critical conditions.
